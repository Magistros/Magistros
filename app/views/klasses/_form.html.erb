<%= form_for(@klass) do |f| %>
  <% if @klass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@klass.errors.count, "error") %> prohibited this klass from being saved:</h2>

      <ul>
      <% @klass.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :jahrgang %><br>
    <%= f.number_field :jahrgang %>
  </div>
  <div class="field">
    <%= f.label :kennzeichnung %><br>
    <%= f.text_field :kennzeichnung %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
